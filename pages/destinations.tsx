import { Navbar } from "@/components/global";

type Props = {};
const Destinations = (props: Props) => {
  return (
    <div>
      <Navbar />
      test
    </div>
  );
};
export default Destinations;
